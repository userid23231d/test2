name: UBUNTU_SERVER
on:
  push:
    branches:
      - main  
jobs:
  my-job:
    name: my-job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a command on Ubuntu
        run: echo "Hello from Ubuntu!"
